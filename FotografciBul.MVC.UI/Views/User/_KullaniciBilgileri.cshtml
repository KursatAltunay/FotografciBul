@model Kullanici

<div class="py-5" id="section-about">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-12 col-lg-4 order-lg-1" data-aos="fade-up">
                <h2 class="heading mb-4">Hoşgeldiniz, @Model.Adi @Model.Soyadi</h2>
                @*<p class="mb-5"><h4>Randevu Talepleriniz:</h4> @Model.RandevuTalebi</p>*@
                @*<p> @Html.ActionLink("BilgileriDuzenle", "Photographer", new { id = Model.ID }, new { @class = "btn btn-warning text-white py-2 mr-3 text-uppercase letter-spacing-1" }) </p>*@
            </div>
        </div>
    </div>
</div>

